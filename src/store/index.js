import store from './store'
import subscribe from './subscribe'
import defaultState from './defaultState'

/**
 * Export default state and the subscribe HOC
 */
export {
  defaultState,
  subscribe
}

/**
 * Export the redux store
 */
export default store
